name: Day 11 CI

on:
  push:
    branches: [main]
    paths:
      - "Day 11/**"
      - ".github/workflows/day11-ci.yaml"
  pull_request:
    paths:
      - "Day 11/**"
      - ".github/workflows/day11-ci.yaml"

jobs:
  day11-backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: "Day 11"
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: tasks_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            Day 11/requirements.txt
            Day 11/requirements-dev.txt
      - name: Install Python dependencies
        run: pip install -r requirements.txt -r requirements-dev.txt
      - name: Run pytest
        env:
          TEST_DATABASE_URL: postgresql+asyncpg://postgres:postgres@localhost:5432/tasks_test
        run: python -m pytest -q

  day11-frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: "Day 11/frontend"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: Day 11/frontend/package-lock.json
      - run: npm ci
      - run: npm run lint
      - run: npm run build
