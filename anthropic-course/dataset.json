[
  {
    "task": "Parse an AWS S3 bucket name from an S3 URI (e.g., 's3://my-bucket/path/to/file.txt') and extract just the bucket name",
    "format": "regex"
  },
  {
    "task": "Create a JSON CloudFormation template snippet that defines an IAM role with a trust policy allowing EC2 to assume it",
    "format": "json"
  },
  {
    "task": "Write a Python function that takes an AWS Lambda event object and extracts the request body from an API Gateway proxy request",
    "format": "python"
  }
]